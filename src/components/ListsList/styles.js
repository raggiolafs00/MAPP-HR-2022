import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    ListsContainer: {
        flexDirection: 'column',
        alignItems: 'center',
        margin: '20%',
        border: '10px',
    }
})