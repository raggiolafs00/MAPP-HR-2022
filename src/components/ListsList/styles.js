import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  ListsContainer: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'space-between',
    paddingTop: 10,
    paddingLeft: 20,
    paddingRight: 20
  }
})
